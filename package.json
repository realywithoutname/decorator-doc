{
  "name": "decorator-doc",
  "version": "1.0.2",
  "description": "A swagger doc generator with decorator syntax for node.js,It surport use a js object define too.",
  "main": "src/index.js",
  "directories": {
    "example": "example"
  },
  "scripts": {
    "demo": "nodemon --exec babel-node example/index.js --watch src --watch example",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node.js",
    "swagger doc",
    "swagger.js",
    "docorator swagger doc",
    "joi",
    "swagger api validate"
  ],
  "author": "liuJD",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/realywithoutname/decorator-doc"
  },
  "dependencies": {
    "config": "^1.26.2",
    "joi-to-json-schema": "^3.0.0",
    "lodash": "^4.17.4",
    "require-dir": "^0.3.2"
  }
}
